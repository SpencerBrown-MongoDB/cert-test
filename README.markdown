Add this line to /etc/hosts: (use an IP address that is equivalent to localhost)

```
127.0.1.2 mongodb-local.computer
```
Now download the tarball for MongoDB Server for the appropriate platform and release. Untar it and put mongod into the path. Now do the same for mongosh.

